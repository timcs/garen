server:
  port: 3239

spring:
  profiles:
    active: dev
  application:
    name: garen-server
  datasource:
    url: jdbc:mysql://localhost:3306/garen
    username: root
    password: 1qaz@WSX
logging:
  level:
    top.binaryx.garen.server.mapper: DEBUG
    root: INFO
